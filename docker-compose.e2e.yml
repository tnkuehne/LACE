services:

  database:
    image: postgis/postgis:16-master
    ports:
      - 5432:5432
    volumes:
      - ./directus/postgres:/var/lib/postgresql/data
    env_file: ".env"
    networks:
      - custom-app-network

  cms:
    image: directus/directus:10.12.1
    ports:
      - 8055:8055
    volumes:
      - ./directus/uploads:/directus/uploads
      - ./directus/extensions:/directus/extensions
      - ./directus/database:/directus/database
    depends_on:
      - database
    env_file: ".env"
    networks:
      - custom-app-network

  web:
    build:
      context: web
    networks:
      - custom-app-network
    ports:
      - "3000:3000"
    env_file: ".env"

  nginx:
    image: nginx:latest
    volumes:
      - ./web/nginx.local.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - web
    networks:
      - custom-app-network

networks:
  custom-app-network: